# 🇮🇳 Indian Name Generator

A simple, fun web app that generates **random Indian names** using plain HTML, CSS, and JavaScript.  
It combines a randomly selected first and last name to produce a realistic Indian name every time you click the button.

---

## 🌟 Features
- Generates a random **Indian first name** and **last name**  
- Clean and colorful design with a smooth gradient background  
- Works entirely offline — no frameworks or dependencies  
- Can be hosted easily using **GitHub Pages**  

---

## 🧩 How It Works
The app uses two JavaScript arrays:
- `firstNames[]` — contains common Indian first names  
- `lastNames[]` — contains popular Indian surnames  

Each time you click **"Generate Name"**, the script randomly picks one name from each array and displays the result.

---

## 🗂️ Project Structure
